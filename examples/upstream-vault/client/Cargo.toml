[package]
name = "upstream-vault-client"
version = "0.1.0"
edition = "2021"

[dependencies]
quasar-lang = { path = "../../../lang" }
wincode = { version = "0.4", features = ["derive"] }
solana-address = "2"
solana-instruction = "3"
